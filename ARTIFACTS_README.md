# 🎨 Website Artifacts - Новая функциональность

## Что было добавлено?

Режим "Artifacts: Website" позволяет генерировать полноценные интерактивные веб-сайты прямо в чате WindexsAI.

### ✨ Основные возможности

- **Генерация через AI**: Пользователь просит создать сайт, OpenAI генерирует полный React-проект
- **Интерактивный редактор**: Sandpack позволяет редактировать код и видеть результат в реальном времени
- **Сохранение**: Все артефакты сохраняются в базе данных и привязываются к сообщениям
- **Мультифайловые проекты**: React + TypeScript + Vite с полной структурой файлов
- **Управление зависимостями**: Автоматическая установка npm пакетов

## 📁 Измененные файлы

### Backend
- ✅ `src/lib/database.js` - добавлена таблица `artifacts`, колонка `artifact_id` в `messages`
- ✅ `server.js` - добавлены endpoints для работы с артефактами и генерации через OpenAI
- ✅ `init-db.js` - работает с новой схемой БД

### Frontend
- ✅ `src/lib/api.ts` - добавлен интерфейс `Artifact` и методы API для артефактов
- ✅ `src/lib/openai.ts` - функции `detectWebsiteIntent()` и `generateWebsiteArtifact()`
- ✅ `src/pages/Chat.tsx` - интеграция генерации и отображения артефактов
- ✅ `src/components/WebsiteArtifactCard.tsx` - новый компонент для рендера артефактов

### Зависимости
- ✅ `@codesandbox/sandpack-react` - библиотека для интерактивного редактора

## 🚀 Быстрый старт

### 1. Установка

```bash
npm install
```

### 2. Инициализация БД

```bash
npm run init-db
```

### 3. Настройка

Убедитесь, что в `.env` настроен:
```
OPENAI_API_KEY=your_api_key_here
```

### 4. Запуск

```bash
npm run dev:full
```

Или отдельно:
```bash
# Терминал 1
npm run server

# Терминал 2
npm run dev
```

## 💬 Как использовать

Просто напишите в чат:

```
Создай сайт для кофейни
```

или

```
Сделай лендинг для IT-стартапа
```

или

```
Создай приложение TODO list
```

## 🔑 Ключевые фразы для активации

- `создай сайт`, `сделай сайт`, `сверстай сайт`
- `создай лендинг`, `сделай лендинг`
- `создай страницу`, `сделай страницу`
- `разработай сайт`
- `website`, `create a website`, `build a website`
- `создай приложение`, `сделай приложение`

## 📊 Архитектура

### База данных

```sql
-- Новая таблица
CREATE TABLE artifacts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id INTEGER NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('website')),
  title TEXT NOT NULL,
  files_json TEXT NOT NULL,
  deps_json TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- Обновленная таблица
ALTER TABLE messages ADD COLUMN artifact_id INTEGER;
```

### API Endpoints

```
POST   /api/artifacts/generate    - Генерация через OpenAI
POST   /api/artifacts             - Создание артефакта
GET    /api/artifacts/:id         - Получение артефакта
PUT    /api/artifacts/:id         - Обновление артефакта
GET    /api/sessions/:id/artifacts - Список артефактов сессии
```

### Поток данных

```
1. Пользователь → "Создай сайт X"
2. Chat.tsx → detectWebsiteIntent() → true
3. Chat.tsx → generateWebsiteArtifact(prompt)
4. OpenAI → возвращает структурированный JSON
5. Chat.tsx → apiClient.createArtifact()
6. Server → DatabaseService.createArtifact()
7. Chat.tsx → loadArtifact() → setArtifacts()
8. Chat.tsx → рендерит WebsiteArtifactCard
9. Sandpack → отображает интерактивный редактор
```

## 📚 Документация

- **[ARTIFACTS_GUIDE.md](./ARTIFACTS_GUIDE.md)** - Полное руководство по использованию
- **[TESTING_ARTIFACTS.md](./TESTING_ARTIFACTS.md)** - Инструкции по тестированию
- **[ARTIFACTS_EXAMPLES.md](./ARTIFACTS_EXAMPLES.md)** - 10+ примеров артефактов

## 🛠️ Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик для Sandpack проектов
- **Sandpack** - интерактивный редактор от CodeSandbox
- **Tailwind CSS** - стилизация артефактов
- **SQLite** - хранение артефактов
- **OpenAI API** - генерация кода

## ⚡ Ограничения

- Максимальный размер проекта: **400KB**
- Поддержка только React + TypeScript проектов
- Обязательные файлы: `/index.html`, `/src/main.tsx`, `/src/App.tsx`

## 🐛 Известные проблемы

### OpenAI иногда возвращает markdown вместо JSON
**Решено:** Автоматическая обработка markdown блоков

### Sandpack не работает в некоторых браузерах
**Решение:** Используйте современные браузеры (Chrome, Firefox, Safari, Edge)

### Артефакт слишком большой
**Решение:** Попросите пользователя упростить запрос

## 🔮 Будущие улучшения

- [ ] Экспорт проекта в ZIP
- [ ] Деплой на Vercel/Netlify одной кнопкой
- [ ] Выбор фреймворка (Vue, Svelte, Angular)
- [ ] Темы/шаблоны для быстрого старта
- [ ] Совместное редактирование
- [ ] История изменений (версионирование)
- [ ] Предпросмотр на мобильных устройствах
- [ ] AI-ассистент для доработки ("добавь форму", "измени цвет")

## 📞 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь, что БД инициализирована
4. Проверьте, что OPENAI_API_KEY настроен

## 📄 Лицензия

MIT License
