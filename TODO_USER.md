# ✅ Что нужно сделать перед запуском

## 1. Установка зависимостей (если еще не сделано)

```bash
npm install
```

## 2. Инициализация базы данных

```bash
npm run init-db
```

**Ожидаемый вывод:**
```
Migrating database: adding artifact_id column to messages table
🗄️  Initializing database...
✅ Created test session with ID: X
✅ Saved test messages: X, Y
✅ Loaded 2 messages from session X
🎉 Database initialized successfully!
```

## 3. Настройка OpenAI API ключа

Убедитесь, что в файле `.env` есть:

```env
OPENAI_API_KEY=sk-...ваш_ключ_здесь
```

Если файла `.env` нет, создайте его:

```bash
echo "OPENAI_API_KEY=sk-your-key-here" > .env
```

## 4. Запуск приложения

### Вариант 1: Всё вместе (рекомендуется)
```bash
npm run dev:full
```

### Вариант 2: Раздельно (для отладки)

**Терминал 1 - Backend:**
```bash
npm run server
```

**Терминал 2 - Frontend:**
```bash
npm run dev
```

**Терминал 3 - MCP сервер (опционально):**
```bash
npm run mcp
```

## 5. Открыть в браузере

Перейдите на: https://ai.windexs.ru

## 6. Протестировать артефакты

В чате напишите:

```
Создай красивый лендинг для кофейни
```

Через 5-10 секунд должен появиться интерактивный сайт с редактором Sandpack! 🎉

---

## 🐛 Если что-то пошло не так

### Ошибка: "OPENAI_API_KEY not configured"
**Решение:** Проверьте файл `.env` и убедитесь, что API ключ настроен

### Ошибка: "Cannot find module @codesandbox/sandpack-react"
**Решение:** Запустите `npm install`

### Ошибка: "no such table: artifacts"
**Решение:** Запустите `npm run init-db`

### Артефакт не создается
**Решение:** 
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь, что OPENAI_API_KEY корректен

### Sandpack не отображается
**Решение:**
1. Очистите кэш браузера
2. Проверьте, что нет блокировщиков рекламы
3. Попробуйте другой браузер (Chrome, Firefox, Safari)

---

## 📚 Дополнительная информация

После успешного запуска:

- **Документация:** См. `ARTIFACTS_README.md`
- **Примеры:** См. `ARTIFACTS_EXAMPLES.md`
- **Тестирование:** См. `TESTING_ARTIFACTS.md`
- **Quick Start:** См. `QUICK_START_ARTIFACTS.md`

---

## ✅ Checklist перед использованием

- [ ] Зависимости установлены (`npm install`)
- [ ] База данных инициализирована (`npm run init-db`)
- [ ] OPENAI_API_KEY настроен в `.env`
- [ ] Сервер запущен (`npm run server` или `npm run dev:full`)
- [ ] Клиент запущен (`npm run dev` или `npm run dev:full`)
- [ ] Браузер открыт на https://ai.windexs.ru
- [ ] Тестовый артефакт создан успешно

---

🎉 **Готово! Наслаждайтесь созданием сайтов!** 🎉
